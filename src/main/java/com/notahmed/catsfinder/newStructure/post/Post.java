package com.notahmed.catsfinder.newStructure.post;

import org.springframework.data.relational.core.mapping.Table;

// TODO create the post table in sql
@Table("Post")
public record Post(

        // create schema first
) { }
