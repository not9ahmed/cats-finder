package com.notahmed.catsfinder.security;

import org.springframework.stereotype.Service;

// class to handle jwt related services such as generate token, get username from token etc
@Service
public class OldJwtService {

    // will have the boilerplate for jwt
    // extract claims
    // generate token etc
    // set the secret key



}
